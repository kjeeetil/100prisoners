server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    location / {
        # Serve the index file
    }

    # Suppress favicon logs
    location = /favicon.ico {
        log_not_found off;
        access_log off;
    }
}
